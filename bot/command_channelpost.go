package bot

import (
	"gopkg.in/telebot.v3"
)

func commandChannelPost(c telebot.Context) error {
	var err error

	// log.Println(c.Message().ID)

	// msg := c.Message()
	// _, err = b.Edit(msg, fb)
	// if err != nil {
	// 	loge(err)
	// }

	return err
}
