package bot

import (
	"gopkg.in/telebot.v3"
)

func commandChannelPost(c telebot.Context) error {
	var err error
	// fb := getFrenlyButton()

	// msg := c.Message()
	// msg, err = b.Edit(msg, fb)
	// if err != nil {
	// 	loge(err)
	// }

	return err
}
